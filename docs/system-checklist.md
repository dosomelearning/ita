# System Checklist

Use this file as the live project backlog for cross-service, architecture, security, and delivery work.

Last Issued ID: `T-001`

## Rules

- Keep each item short and outcome-focused.
- Use one status only: `todo`, `in_progress`, or `blocked`.
- Use single ID format: `T-###` (for example, `T-004`).
- When adding an item, increment from `Last Issued ID` and then update `Last Issued ID`.
- When an item is completed and committed, remove it from this table; completion history is tracked in git log and commit messages.
- Commit messages should reference related `T-###` IDs.
- Create a detailed task file in `docs/tasks/` only when an item needs more than a short backlog row.

## Active Items

| ID | Status | Area | Task | Links | Notes |
|---|---|---|---|---|---|
| T-001 | in_progress | Docs | Standardizing project documentation and task management. | `AGENTS.md`, `docs/README.md`, `docs/system-checklist.md`, `docs/tasks/README.md`, `docs/tasks/TEMPLATE.md` | Pending commit |
